# dati riservati / configurazione locale
.env
.streamlit/secrets.toml

# PDF e documenti reali caricati dall'utente (mai committare dati reali)
uploads/
tmp/

# regole modificate in locale quando GitHub non è configurato
# (di norma le regole vivono nel repo sotto regole/, ma il fallback locale
# scrive anche in config/rules_*.json: se lo usi solo come fallback,
# scommenta la riga sotto per non versionarlo)
# config/rules_*.json

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# editor / OS
.vscode/
.idea/
.DS_Store

# PDF e documenti reali (mai committare dati reali/sensibili)
*.pdf
