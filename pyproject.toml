[project]
name = "prima-nota-paghe-f24"
version = "0.2.0"
description = "Estrazione da PDF paghe/F24 e generazione XML per import in GB Software"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
